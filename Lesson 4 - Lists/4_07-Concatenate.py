orders = ['daisy', 'buttercup', 'snapdragon', 'gardenia', 'lily']

# Lists can be concatenated to other lists
new_orders = orders + ['lilac', 'iris']

# In order to concatenate a single item, it still needs to be a list with one element
broken_prices = [5, 3, 4, 5, 4] + [4]
