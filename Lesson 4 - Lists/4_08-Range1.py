# You can use range() to build a list of sequential numbers
list1 = range(9)

# Using range() produces a range object
print(list1)
print(type(list1))

# The range object can be converted to a list with list()
list1 = list(list1)

print(list1)
