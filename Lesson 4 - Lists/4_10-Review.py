# Creating a list
first_names = ['Ainsley', 'Ben', 'Chani', 'Depak']

# Initializing an empty list to append to later
age = []

# Add a value to the end of the list
age.append(42)

# Concatenate one list with another
all_ages = [32, 41, 29] + age

# Combine two lists into a zip object
name_and_age = zip(first_names, all_ages)

# Create a range object with sequential numbers
ids = range(4)
